.skill {
    position: relative;
}

.skill__title {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.skill__circle {
    width: 150px;
    height: 150px;
}
